server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: utf-8
      enabled: true
      force: true
  tomcat:
    uri-encoding: UTF-8
spring:
  config:
    activate:
      on-profile:
        - local
  application:
    name: SpringCiCdTest